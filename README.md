# LeadHunter — AI Поиск клиентов

**LeadHunter** — это веб-сервис для автоматизации поиска новых клиентов и рассылки персонализированных коммерческих предложений.

## Возможности

- 🔍 **Автоматический поиск компаний** — по нише и городу через Tavily Search API + парсинг email-адресов с сайтов
- 🤖 **AI-генерация писем** — уникальное персонализированное КП для каждой компании через OpenAI GPT-4o
- 📧 **Рассылка** — индивидуальная отправка каждому адресату (не BCC), с PDF-вложением
- 📊 **CRM-статусы** — воронка: Новый → Отправлено → Ответили → В работе / Заинтересован / Отказ
- 💬 **Переписка** — журнал входящих и исходящих сообщений по каждой компании

## Стек технологий

| Слой | Технологии |
|---|---|
| Бэкенд | Python 3.10+, FastAPI, SQLAlchemy, SQLite |
| Фронтенд | Vanilla HTML/CSS/JS |
| AI | OpenAI GPT-4o / GPT-4o-mini |
| Поиск | Tavily Search API + httpx (веб-скрейпинг) |
| Email | SMTP / Gmail (DEMO mode) |

## Быстрый старт

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/your-username/leadhunter.git
cd leadhunter
```

### 2. Создайте файл Credentials.env
```bash
cp Credentials.env.example Credentials.env
```
Откройте `Credentials.env` и заполните ваши ключи API.

### 3. Запустите проект
```
run.bat
```
или вручную:
```bash
pip install -r requirements.txt
python -m uvicorn backend.main:app --reload --port 8000
```

### 4. Откройте в браузере
```
http://127.0.0.1:8000
```

## Получение API-ключей

| Сервис | Где получить | Стоимость |
|---|---|---|
| **OpenAI** | https://platform.openai.com/api-keys | Платный (GPT-4o) |
| **Tavily** | https://tavily.com | Бесплатный план: 1000 запросов/мес |

## Режим демо

В `Credentials.env` установите `DEMO_MODE=true` — письма будут генерироваться, но **не отправляться** реальным получателям. Статусы обновляются в штатном режиме.

## Структура проекта

```
leadhunter/
├── backend/
│   ├── main.py          # FastAPI endpoints
│   ├── database.py      # SQLAlchemy модели
│   ├── search_agent.py  # Поиск компаний + парсинг email
│   ├── email_generator.py # AI-генерация писем
│   ├── email_sender.py  # Отправка через SMTP
│   └── pdf_generator.py # Генерация PDF-каталога
├── frontend/
│   ├── index.html       # UI (всё в одном файле)
│   └── app.js           # JavaScript логика
├── Credentials.env.example
├── requirements.txt
├── run.bat
└── README.md
```
